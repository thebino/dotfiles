---
actions:
  - action: package.install
    name: alacritty

  - action: file.copy
    where: os.name == "linux" || os.name == "macos"
    from: alacritty.yaml
    to: "{{ user.home_dir }}/.config/alacritty/alacritty.yaml"

  - action: file.copy
    where: os.name == "linux" || os.name == "macos"
    from: colors.yaml
    template: true
    to: "{{ user.home_dir }}/.config/alacritty/colors.yaml"

  - action: file.copy
    where: os.name == "linux" || os.name == "macos"
    from: fonts.yaml
    template: true
    to: "{{ user.home_dir }}/.config/alacritty/fonts.yaml"
