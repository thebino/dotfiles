actions:
  - action: package.install
    name: helix

  - action: file.copy
    from: config.toml
    template: true
    to: "{{ user.config_dir }}/helix/config.toml"

  - action: file.copy
    from: languages.toml
    template: true
    to: "{{ user.config_dir }}/helix/languages.toml"
