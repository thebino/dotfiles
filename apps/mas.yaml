# Mac App Store command-line interface
actions:
  - action: package.install
    where: os.name == "macos"
    name: mas

  - action: command.run
    where: os.name == "macos"
    command: mas
    args:
      - upgrade
