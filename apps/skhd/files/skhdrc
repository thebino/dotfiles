# https://github.com/koekeishiya/skhd/issues/1
# modifiers
# shift, alt, ctrl, hyper (cmd + shift + alt + ctrl)
# 
# key literals
# "return", "tab", "space", "backspace", "escape"

alt - w : open /Applications/Firefox.app
alt - t : open /Applications/WezTerm.app
alt - m : open /Applications/Microsoft\ Outlook.app
alt - s : open /Applications/Slack.app

# focus within space
# alt - down : yabai -m window --focus south
# alt - up : yabai -m window --focus north
# alt - left : yabai -m window --focus west
# alt - right : yabai -m window --focus east

# Move
# shift + alt - 1 : yabai -m window --space 1;
# shift + alt - 2 : yabai -m window --space 2;
# shift + alt - 3 : yabai -m window --space 3;
# shift + alt - 4 : yabai -m window --space 4;
# shift + alt - 5 : yabai -m window --space 5;
# shift + alt - 6 : yabai -m window --space 6;
# shift + alt - 7 : yabai -m window --space 7;

# alt + . : yabai -m window --toggle float --grid 4:4:1:1:2:2
