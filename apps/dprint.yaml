# code formatter
# https://dprint.dev
actions:
  - action: package.install
    name: dprint

  - action: command.run
    command: dprint
    args:
      - "init"
      - "--config"
      - "{{ user.home_dir }}/.config/dprint.json"
